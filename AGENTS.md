# AGENTS.md

This project keeps a single agent-guidance document: **[CLAUDE.md](CLAUDE.md)**.

It covers what the tool measures and why, the architecture, the invariants that
must not be simplified away (origin weights, the class-lists-only transform,
idempotency, the corpus twins), and how to add a metric.

Two files that overlapped heavily used to be kept in sync by hand; they drifted.
One file cannot.
